{
  "name": "smooth-scroll",
  "description": "Smooth scrolling for text editors via mouse and keyboard",
  "repository": "https://github.com/asiloisad/pulsar-smooth-scroll",
  "keywords": [
    "smooth",
    "scroll",
    "mouse",
    "keyboard"
  ],
  "version": "2.1.0",
  "license": "MIT",
  "engines": {
    "atom": ">=1.100.0 <2.0.0"
  },
  "main": "./lib/main",
  "configSchema": {
    "scrollComputation": {
      "order": 1,
      "title": "Scroll computation",
      "description": "Choose `time-based` for frame-rate independent target scrolling, or `divider` for the legacy frame-based divider method.",
      "type": "string",
      "enum": [
        "time-based",
        "divider"
      ],
      "default": "time-based"
    },
    "scrollAllWithCtrl": {
      "order": 2,
      "title": "Enable Ctrl multi-editor scroll",
      "description": "Use `Ctrl` to scroll all visible text editors",
      "type": "boolean",
      "default": true
    },
    "useAltMultiplier": {
      "order": 3,
      "title": "Enable Alt wheel multiplier",
      "description": "Use `Alt` to apply the alternative mouse wheel distance multiplier",
      "type": "boolean",
      "default": true
    },
    "horizontalWithShift": {
      "order": 4,
      "title": "Enable Shift horizontal scroll",
      "description": "Use `Shift` to change scroll to horizontal direction",
      "type": "boolean",
      "default": true
    },
    "reverseWheelDirection": {
      "order": 5,
      "title": "Reverse mouse wheel direction",
      "description": "Invert vertical and horizontal mouse wheel scroll direction",
      "type": "boolean",
      "default": false
    },
    "wheelSmoothness": {
      "order": 6,
      "title": "Mouse wheel smoothness",
      "description": "Controls mouse wheel animation. Lower is faster; higher is smoother. In divider mode this is the legacy wheel divider.",
      "type": "number",
      "minimum": 1,
      "maximum": 20,
      "default": 8
    },
    "wheelMultiplier": {
      "order": 7,
      "title": "Mouse wheel distance multiplier",
      "description": "Scales the scroll distance generated by mouse wheel events",
      "type": "number",
      "minimum": 0.1,
      "maximum": 10,
      "default": 1.5
    },
    "altWheelMultiplier": {
      "order": 8,
      "title": "Alt mouse wheel distance multiplier",
      "description": "Scales mouse wheel distance while `Alt` is held",
      "type": "number",
      "minimum": 0.1,
      "maximum": 20,
      "default": 7.5
    },
    "commandSmoothness": {
      "order": 9,
      "title": "Scroll command smoothness",
      "description": "Controls keyboard scroll command animation. Lower is faster; higher is smoother. In divider mode this is the legacy command divider.",
      "type": "number",
      "minimum": 1,
      "maximum": 50,
      "default": 12
    },
    "commandDistance": {
      "order": 10,
      "title": "Scroll command distance multiplier",
      "description": "Scales the distance of keyboard scroll commands relative to editor height",
      "type": "number",
      "minimum": 0.015625,
      "maximum": 64,
      "default": 1
    }
  }
}
